# Dealer Integration Project

# Requirements

* Java 15

# Test, Build

In oder to test and build(with gradlew) this project, you need to run below command:

```bash
./gradlew clean test build
```

# Run

```bash
java -jar ./build/libs/dealer-integration-0.0.1-SNAPSHOT.jar
```

